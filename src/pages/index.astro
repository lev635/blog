---
import type { MarkdownInstance } from "astro";

import Head from "../components/Head.astro";
import Navigation from "../components/Navigation.astro";
import ArticleListReact from "../components/ArticleListReact";
import Footer from "../components/Footer.astro";
import "/src/styles/tailwind.css";

const articleFiles = import.meta.glob("../pages/article/*.md", { eager: true} );
const articles = Object.values(articleFiles) as MarkdownInstance<Record<string, any>>[];
---

<Head title="ゆかり"/>
<Navigation />
<article>
  <h1 class="font-semibold">新着記事</h1>
  <ArticleListReact articles={articles} limit={3} />
</article>
<Footer />
